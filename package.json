{
  "name": "tpulse-chrome-extension",
  "private": true,
  "version": "1.0.0-beta.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:proxy": "cd src-proxy && cargo build --release"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.10.0",
    "typescript": "^5.3.2",
    "vite": "^5.0.0",
    "vite-plugin-web-extension": "^4.0.0"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  }
}
